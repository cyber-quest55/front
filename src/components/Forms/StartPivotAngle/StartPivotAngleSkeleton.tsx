import { useIntl } from '@umijs/max';
import { Typography } from 'antd';
import * as React from 'react';


const StartPivotAngleSkeleton: React.FunctionComponent<any> = (props) => {
  const intl = useIntl();

  return <Typography.Link disabled style={{ width: '100%' }}>
   sadsa
  </Typography.Link>;
};

export default StartPivotAngleSkeleton;
