import * as React from 'react';

interface IStartPivotScheduleMobileProps {
}

const StartPivotScheduleMobile: React.FunctionComponent<IStartPivotScheduleMobileProps> = (props) => {
  return ;
};

export default StartPivotScheduleMobile;
