import * as React from 'react';

interface IRadioInputSkeletonProps {
}

const RadioInputSkeleton: React.FunctionComponent<IRadioInputSkeletonProps> = (props) => {
  return ;
};

export default RadioInputSkeleton;
