import * as React from 'react';

interface IRadioInputMobileProps {
}

const RadioInputMobile: React.FunctionComponent<IRadioInputMobileProps> = (props) => {
  return ;
};

export default RadioInputMobile;
