export enum DeviceType {
  'Meter' = 'meter',
  'Pivot' = 'pivot',
  'Repeater' = 'repeater',
  'Pump' = 'pump',
}

export enum PumpHistoryOrigin {
  'Command' = 0,
  'CentralUpdate' = 1,
}

export enum MeterSystemOrigin {}
